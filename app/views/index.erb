<div class="row">
  <div class ="slider">
  <div id="slider">
   <figure>
    <% @products.each do |product| %>
      <% product.images.each do |image| %>
        <img src="<%= image.file_path %>">
      <% end %>
    <% end %>
   </figure>
  </div>
  </div>
</div>

<div id="banner">
  <img src="/images/logo/pamoja_white.png">
</div>